# Runtime Game Code

`game/` contains shippable scenes, scripts, Resources, and UI.

Introduce subdirectories as working systems are implemented. Follow the boundaries in `docs/ARCHITECTURE.md`; do not create a large empty folder hierarchy or place editor-only tools in runtime code.
