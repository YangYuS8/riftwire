# Tests

`tests/` contains cross-system unit, integration, replay, room-validation, and performance fixtures.

The concrete Godot test framework is intentionally deferred until the engine minor version is pinned. Test design and evidence requirements are defined in `docs/TESTING.md`.
